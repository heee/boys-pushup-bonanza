# Ladder rivals

## Open voice item

Competitor callouts include live user names and therefore currently use the existing `speechSynthesis` fallback. A future voice-generation pass should add a safe way to generate or compose dynamic name clips with the app's OpenAI voice without requiring a new app release whenever the group changes.
